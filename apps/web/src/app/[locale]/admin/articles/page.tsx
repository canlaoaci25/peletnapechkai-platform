import{redirect}from"next/navigation";import{AdminDashboard}from"@/components/admin/admin-dashboard";import{ArticleEditor}from"@/components/admin/article-editor";import{adminCopy}from"@/i18n/admin-copy";import{hasLocale}from"@/i18n/config";import{getAdminSession,getArticles,getSystemStatus}from"@/lib/admin-api";
export default async function ArticlesPage({params}:PageProps<"/[locale]/admin/articles">){const{locale}=await params;if(!hasLocale(locale))redirect("/tr-TR/admin/login");const session=await getAdminSession();if(!session)redirect(`/${locale}/admin/login`);const[articles,status]=await Promise.all([getArticles(),session.roles.some(x=>["Owner","Admin"].includes(x))?getSystemStatus():Promise.resolve(null)]);return <main className="admin-shell admin-dashboard-shell"><header className="admin-command-header"><div><p className="section-kicker">YAYIN YÖNETİMİ</p><h1>İçerikler</h1><p>Arama, filtreleme, düzenleme ve yeni içerik işlemleri.</p></div></header><AdminDashboard locale={locale} articles={articles} status={status}/><details id="new-article" className="admin-panel create-drawer"><summary><span><strong>Yeni Türkçe içerik oluştur</strong><small>Taslak editörünü aç</small></span><span>+</span></summary><div className="create-drawer-body"><ArticleEditor copy={adminCopy[locale]}/></div></details></main>}
