import{redirect}from"next/navigation";import{AdminDashboard}from"@/components/admin/admin-dashboard";import{hasLocale}from"@/i18n/config";import{getAdminSession,getArticles,getSystemStatus}from"@/lib/admin-api";
export default async function ArticlesPage({params}:PageProps<"/[locale]/admin/articles">){const{locale}=await params;if(!hasLocale(locale))redirect("/tr-TR/admin/login");const session=await getAdminSession();if(!session)redirect(`/${locale}/admin/login`);const[articles,status]=await Promise.all([getArticles(),session.roles.some(x=>["Owner","Admin"].includes(x))?getSystemStatus():Promise.resolve(null)]);return <main className="admin-shell admin-dashboard-shell"><header className="admin-command-header"><div><p className="section-kicker">YAYIN YÖNETİMİ</p><h1>İçerikler</h1><p>İçerikleri arayın, duruma göre filtreleyin ve düzenleyin.</p></div></header><AdminDashboard locale={locale} articles={articles} status={status}/></main>}
