import{redirect}from"next/navigation";import{AdminOverview}from"@/components/admin/admin-overview";import{AutomaticContentControl}from"@/components/admin/automatic-content-control";import{siteConfig}from"@/config/site";import{hasLocale}from"@/i18n/config";import{getAdminSession,getArticles,getSystemStatus}from"@/lib/admin-api";
export default async function AdminPage({params}:PageProps<"/[locale]/admin">){const{locale}=await params;if(!hasLocale(locale))redirect("/tr-TR/admin/login");const session=await getAdminSession();if(!session)redirect(`/${locale}/admin/login`);const canManage=session.roles.some(x=>["Owner","Admin"].includes(x));const[articles,status]=await Promise.all([getArticles(),canManage?getSystemStatus():Promise.resolve(null)]);return <main className="admin-shell admin-dashboard-shell"><header className="admin-command-header"><div><p className="section-kicker">{siteConfig.name} / İSTATİSTİKLER</p><h1>Kontrol merkezi</h1><p>Yayın performansı, içerik dağılımı ve sistem sağlığı.</p></div><div className="dashboard-live"><span/><strong>Canlı sistem</strong></div></header>{canManage&&<AutomaticContentControl/>}<AdminOverview locale={locale} articles={articles} status={status}/></main>}
