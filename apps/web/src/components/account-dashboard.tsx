"use client";
import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";import type{MemberAccount}from"@/lib/admin-api";
export function AccountDashboard({account}:{account:MemberAccount}){const router=useRouter(),[message,setMessage]=useState("");async function send(path:string,body:unknown){setMessage("");const csrf=await fetch("/api/admin/auth/csrf",{cache:"no-store"}),{token}=await csrf.json() as {token:string};const response=await fetch(`/api/admin/account/${path}`,{method:path==="profile"?"PUT":"POST",headers:{"content-type":"application/json","x-csrf-token":token},body:JSON.stringify(body)});setMessage(response.ok?"Değişiklik kaydedildi.":"İşlem tamamlanamadı. Bilgileri kontrol edin.");if(response.ok)router.refresh()}function profile(event:FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget);void send("profile",{displayName:data.get("displayName")})}function password(event:FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget);void send("password",{currentPassword:data.get("currentPassword"),newPassword:data.get("newPassword")})}return <div className="member-dashboard"><section className="account-summary"><p className="section-kicker">BOECL ÜYELİK</p><h1>Hesabım</h1><p>{account.email}</p><span className={account.emailConfirmed?"verified":"pending"}>{account.emailConfirmed?"E-posta doğrulandı":account.verificationAvailable?"E-posta doğrulaması bekliyor":"E-posta sağlayıcısı henüz yapılandırılmadı"}</span></section><div className="member-settings"><form className="account-settings-card" onSubmit={profile}><h2>Profil</h2><label>Görünen ad<input name="displayName" defaultValue={account.displayName} required minLength={2} maxLength={160}/></label><button>Profili kaydet</button></form><form className="account-settings-card" onSubmit={password}><h2>Parola değiştir</h2><label>Mevcut parola<input type="password" name="currentPassword" required autoComplete="current-password"/></label><label>Yeni parola<input type="password" name="newPassword" required minLength={14} autoComplete="new-password"/></label><button>Parolayı değiştir</button></form></div>{message&&<p className="account-message" role="status">{message}</p>}</div>}
