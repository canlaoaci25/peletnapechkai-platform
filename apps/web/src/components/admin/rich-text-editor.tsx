"use client";
import{EditorContent,useEditor}from"@tiptap/react";import StarterKit from"@tiptap/starter-kit";import Image from"@tiptap/extension-image";import Youtube from"@tiptap/extension-youtube";import Link from"@tiptap/extension-link";import Placeholder from"@tiptap/extension-placeholder";import{useState}from"react";
export function RichTextEditor({name,initialValue,label}:{name:string;initialValue?:string;label:string}){const[value,setValue]=useState(initialValue??"");const editor=useEditor({immediatelyRender:false,extensions:[StarterKit,Image.configure({allowBase64:false}),Youtube.configure({controls:true,nocookie:true}),Link.configure({openOnClick:false,autolink:true}),Placeholder.configure({placeholder:"İçeriğinizi yazmaya başlayın…"})],content:initialValue??"",onUpdate:({editor:e})=>setValue(e.getHTML())});if(!editor)return <label>{label}<textarea name={name} defaultValue={initialValue} rows={12}/></label>;const ask=(message:string)=>window.prompt(message)?.trim();return <div className="rich-editor-field"><span className="rich-editor-label">{label}</span><div className="rich-editor-toolbar" role="toolbar" aria-label="İçerik biçimlendirme"><button type="button" onClick={()=>editor.chain().focus().toggleBold().run()}><b>K</b></button><button type="button" onClick={()=>editor.chain().focus().toggleItalic().run()}><i>İ</i></button><button type="button" onClick={()=>editor.chain().focus().toggleHeading({level:2}).run()}>H2</button><button type="button" onClick={()=>editor.chain().focus().toggleHeading({level:3}).run()}>H3</button><button type="button" onClick={()=>editor.chain().focus().toggleBulletList().run()}>• Liste</button><button type="button" onClick={()=>editor.chain().focus().toggleOrderedList().run()}>1. Liste</button><button type="button" onClick={()=>editor.chain().focus().toggleBlockquote().run()}>Alıntı</button><button type="button" onClick={()=>editor.chain().focus().toggleCodeBlock().run()}>Kod</button><button type="button" onClick={()=>{const url=ask("Bağlantı adresi");if(url)editor.chain().focus().setLink({href:url}).run()}}>Bağlantı</button><button type="button" onClick={()=>{const src=ask("Görsel adresi");if(src)editor.chain().focus().setImage({src,alt:""}).run()}}>Resim</button><button type="button" onClick={()=>{const src=ask("YouTube adresi");if(src)editor.commands.setYoutubeVideo({src})}}>Video</button><button type="button" onClick={()=>editor.chain().focus().undo().run()}>↶</button><button type="button" onClick={()=>editor.chain().focus().redo().run()}>↷</button></div><EditorContent editor={editor} className="rich-editor-content"/><textarea hidden readOnly name={name} value={value}/><small>Başlık, liste, bağlantı, resim, YouTube video, alıntı ve kod bloğu desteklenir.</small></div>}
