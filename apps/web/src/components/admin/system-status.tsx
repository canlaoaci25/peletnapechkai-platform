import type {SystemStatus as Status} from "@/lib/admin-api";
export function SystemStatus({status}:{status:Status}){const gb=(value:number)=>(value/1024/1024/1024).toFixed(1);return <section className="admin-panel status-panel"><h2>System status</h2><dl><div><dt>Database</dt><dd>{status.database}</dd></div><div><dt>Articles / published</dt><dd>{status.articles} / {status.published}</dd></div><div><dt>Users</dt><dd>{status.users}</dd></div><div><dt>Media</dt><dd>{status.mediaFiles} · {(status.mediaBytes/1024/1024).toFixed(1)} MB</dd></div><div><dt>Free disk</dt><dd>{gb(status.diskFreeBytes)} GB</dd></div></dl><time dateTime={status.checkedAt}>{new Date(status.checkedAt).toLocaleString()}</time></section>}
