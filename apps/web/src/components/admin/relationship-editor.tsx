"use client";
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
import type {ArticleDetail,MediaItem,SupportingLibrary} from "@/lib/admin-api";

export function RelationshipEditor({article,library,media,labels}:{article:ArticleDetail;library:SupportingLibrary;media:MediaItem[];labels:Record<string,string>}){const router=useRouter();const [message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();try{const data=new FormData(event.currentTarget);const csrf=await fetch("/api/admin/auth/csrf",{cache:"no-store"});const {token}=await csrf.json() as {token:string};const response=await fetch(`/api/admin/articles/${article.id}/relationships`,{method:"PUT",headers:{"content-type":"application/json","x-csrf-token":token},body:JSON.stringify({categoryIds:data.getAll("categoryIds"),tagIds:data.getAll("tagIds"),authorIds:data.getAll("authorIds"),sourceIds:data.getAll("sourceIds"),mediaAssetIds:data.getAll("mediaAssetIds")})});if(!response.ok)throw new Error();setMessage(labels.saved);router.refresh()}catch{setMessage(labels.error)}}const checks=(name:string,items:{id:string;name?:string;displayName?:string;fileName?:string}[],selected:string[])=><div className="relationship-checks">{items.map(item=><label className="check-label" key={item.id}><input type="checkbox" name={name} value={item.id} defaultChecked={selected.includes(item.id)}/>{item.name??item.displayName??item.fileName}</label>)}</div>;return <form className="admin-form relationship-editor" onSubmit={submit}><h2>{labels.title}</h2><fieldset><legend>{labels.categories}</legend>{checks("categoryIds",library.categories.filter(x=>x.locale===article.locale),article.categoryIds)}</fieldset><fieldset><legend>{labels.tags}</legend>{checks("tagIds",library.tags.filter(x=>x.locale===article.locale),article.tagIds)}</fieldset><fieldset><legend>{labels.authors}</legend>{checks("authorIds",library.authors,article.authorIds)}</fieldset><fieldset><legend>{labels.sources}</legend>{checks("sourceIds",library.sources,article.sourceIds)}</fieldset><fieldset><legend>{labels.media}</legend>{checks("mediaAssetIds",media,article.mediaAssetIds)}</fieldset><button>{labels.save}</button>{message&&<p role="status">{message}</p>}</form>}
